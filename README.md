# Coffee Bot

A simple Telegram bot for ordering coffee. The project is written in Python and uses [Aiogram](https://github.com/aiogram/aiogram).

## CI/CD

The repository includes a GitHub Actions workflow located at `.github/workflows/ci.yml`.
It checks Python files for syntax errors and runs any available tests on every push or pull request to the `main` branch.
